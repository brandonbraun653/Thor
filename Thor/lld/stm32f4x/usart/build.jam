# ====================================================
# Local Rules 
# ====================================================
local rule explicit_alias ( name : sources * : requirements * : default-build * : usage-requirements * )
    {
    alias $(name) : $(sources) : $(requirements) : $(default-build) : $(usage-requirements) ;
    explicit $(name) ;
    }

local path-constant dbg_install_dir = $(ARTIFACTS_DIR)/debug/lld/stm32f4/usart ;
local path-constant rel_install_dir = $(ARTIFACTS_DIR)/release/lld/stm32f4/usart ;

# ====================================================
# Public Targets
# ====================================================
explicit_alias PUB : : : : <define>THOR_LLD_USART ;

# ====================================================
# Static Libraries
# ====================================================
lib lld_usart_chip
  : variant/hw_usart_register_stm32f446xx.cpp

  : <Thor>enabled-F446
    <link>static
    <use>/CHIMERA//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <use>/THOR/LLD/STM32F4//PUB
    <use>/THOR/LLD/STM32F4/USART//PUB
  ;
explicit lld_usart_chip ;
explicit_alias DEVICE_TARGET : lld_usart_chip : : : <library>lld_usart_chip ;

lib lld_usart
  : [ glob *.cpp ]

  : <link>static
    <use>/CHIMERA//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <use>/THOR/LLD/STM32F4//PUB
    <use>/THOR/LLD/STM32F4/USART//PUB
  ;
explicit lld_usart ;
explicit_alias LIB : lld_usart lld_usart_chip : : : <library>lld_usart <library>lld_usart_chip ;

# ====================================================
# Test Driver Executable
# ====================================================
exe lld_usart_test
  : [ glob test/*.cpp ]
    
    /CHIMERA/ALGORITHM//LIB 
    /CHIMERA/COMMON//LIB 
    /CHIMERA/SYSTEM//LIB
    /CHIMERA/THREADING//LIB
    /GOOGLE//gtest
    /THOR/HLD/GPIO//LIB
    /THOR/HLD/SPI//LIB
    /THOR/HLD/SYSTEM//LIB
    /THOR/LLD/STM32F4/FLASH//LIB
    /THOR/LLD/STM32F4/GPIO//LIB
    /THOR/LLD/STM32F4/NVIC//LIB
    /THOR/LLD/STM32F4/POWER//LIB
    /THOR/LLD/STM32F4/RCC//LIB
    /THOR/LLD/STM32F4/SPI//LIB
    /THOR/LLD/STM32F4/USART//LIB
  
  : <link>static
    <Thor>enabled-F4
    <use>/AURORA//PUB
    <use>/CHIMERA//PUB
    <use>/GOOGLE//TEST_PUB
    <use>/THOR//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <linkflags>-Wl,--no-as-needed
  ;
explicit lld_usart_test ;
explicit_alias TEST : lld_usart_test : : : <source>lld_usart_test ;

# ====================================================
# Installation Targets
# ====================================================
install INSTALL_TEST : lld_usart_test
  : <variant>release:<location>$(rel_install_dir)
    <variant>debug:<location>$(dbg_install_dir)
  ;

install INSTALL_LIB : lld_usart lld_usart_chip
  : <variant>release:<location>$(rel_install_dir)
    <variant>debug:<location>$(dbg_install_dir)
  ;

explicit_alias INSTALL_ALL : INSTALL_TEST INSTALL_LIB ;
