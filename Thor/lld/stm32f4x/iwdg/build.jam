# ====================================================
# Local Rules 
# ====================================================
local rule explicit_alias ( name : sources * : requirements * : default-build * : usage-requirements * )
    {
    alias $(name) : $(sources) : $(requirements) : $(default-build) : $(usage-requirements) ;
    explicit $(name) ;
    }

local path-constant dbg_install_dir = $(ARTIFACTS_DIR)/debug/lld/stm32f4/iwdg ;
local path-constant rel_install_dir = $(ARTIFACTS_DIR)/release/lld/stm32f4/iwdg ;

# ====================================================
# Public Targets
# ====================================================
explicit_alias PUB : : : : <define>THOR_LLD_IWDG ;

# ====================================================
# Static Libraries
# ====================================================
lib lld_iwdg_chip
  : variant/hw_iwdg_register_stm32f446xx.cpp

  : <Thor>enabled-F446
    <link>static
    <use>/CHIMERA//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <use>/THOR/LLD/STM32F4//PUB
    <use>/THOR/LLD/STM32F4/IWDG//PUB
  ;
explicit lld_iwdg_chip ;
explicit_alias DEVICE_TARGET : lld_iwdg_chip : : : <library>lld_iwdg_chip ;

lib lld_iwdg
  : [ glob *.cpp ]

  : <link>static
    <use>/CHIMERA//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <use>/THOR/LLD/STM32F4//PUB
    <use>/THOR/LLD/STM32F4/IWDG//PUB
  ;
explicit lld_iwdg ;
explicit_alias LIB : lld_iwdg lld_iwdg_chip : : : <library>lld_iwdg <library>lld_iwdg_chip ;

# ====================================================
# Test Driver Executable
# ====================================================
exe lld_iwdg_test
  : [ glob test/*.cpp ]
    
    /THOR/LLD/STM32F4//TEST_LIB
  
  : <link>static
    <Thor>enabled-F4
    <use>/AURORA//PUB
    <use>/CHIMERA//PUB
    <use>/GOOGLE//TEST_PUB
    <use>/THOR//PUB
    <use>/THOR//TARGET_PROPAGATED_OPTIONS
    <use>/THOR/LLD/STM32F4//TEST_PUB
    <linkflags>-Wl,--no-as-needed
  ;
explicit lld_iwdg_test ;
explicit_alias TEST : lld_iwdg_test : : : <source>lld_iwdg_test ;

# ====================================================
# Installation Targets
# ====================================================
install INSTALL_TEST : lld_iwdg_test
  : <variant>release:<location>$(rel_install_dir)
    <variant>debug:<location>$(dbg_install_dir)
  ;

install INSTALL_LIB : lld_iwdg lld_iwdg_chip
  : <variant>release:<location>$(rel_install_dir)
    <variant>debug:<location>$(dbg_install_dir)
  ;

explicit_alias INSTALL_ALL : INSTALL_TEST INSTALL_LIB ;
